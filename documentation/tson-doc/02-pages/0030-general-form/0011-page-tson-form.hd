@normal page(name:"General Form"){
    @header {
        text("General Form"),
    }
    @body{
        ul{
            "
            TSON supports also complex constructs like arrays, uplets,
            function calls and named objects"
            "To an extent, it even supports expressions"
            "
            It provides consistent Metadata support with
            annotations and parsable comments"
            "UTF8 compliant while supporting char and binary streams"
        }
    }
    
}